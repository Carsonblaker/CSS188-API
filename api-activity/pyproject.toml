[project]
name = "api-activity"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Carson Blaker" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "flask-bcrypt>=1.0.1",
    "flask-restful>=0.3.10",
    "flask-talisman>=1.1.0",
]

[project.scripts]
api-activity = "api_activity:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
